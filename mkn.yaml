

name: gl.glew
version: master
properties:
    github: http://www.github.com/Dekken
scm: ${github}/gl.glew.git

include: ./include
source:  ./src
dependencies:
  - name: gl.gl
    version: master
    scm: ${github}/gl.gl.git

lang: c
args: -DGLEW_STATIC
