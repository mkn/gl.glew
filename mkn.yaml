

name: gl.glew
version: master
properties:
    github: http://www.github.com
scm: ${github}/Dekken/gl.glew.git

include: ./include
source:  ./src
dependencies:
  - name: khronos.OpenGL
    version: master
    scm:  ${github}/rant/khronos.OpenGL.git

lang: c
